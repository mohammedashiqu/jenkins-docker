pipeline {
    agent any

    stages {
        stage('Build docker image') {
            steps {
                withDockerContainer('ashiqummathoor/myapp') {
    // some block
}
            }
        }
    }
}
